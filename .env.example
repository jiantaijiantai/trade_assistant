# trade_assistant local environment example.
# Copy this file to .env and fill in local values only when model-backed features are enabled.

DASHSCOPE_API_KEY=your-dashscope-api-key
DASHSCOPE_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
DASHSCOPE_NATIVE_BASE_URL=https://dashscope.aliyuncs.com/api/v1
LLM_MODEL=qwen3.6-plus
EMBEDDING_MODEL=qwen2.5-vl-embedding

# Runtime behavior
APP_ENV=local
APP_AUTH_MODE=development
LOG_LEVEL=INFO
BUSINESS_CAPABILITY_STAGE=readonly
ALLOW_WRITE_TOOLS=false
ALLOW_HIGH_RISK_TOOLS=false

# Production identity is resolved from request headers:
# X-Tenant-ID, X-User-ID, X-Roles, X-Department-IDs, X-Groups, X-Clearance-Level.
# Set APP_AUTH_MODE=production to reject requests without authenticated identity headers.

EMBEDDING_PROVIDER=local_hash
EMBEDDING_API_KEY=
EMBEDDING_BASE_URL=
EMBEDDING_MODEL=qwen2.5-vl-embedding
EMBEDDING_DIM=512
